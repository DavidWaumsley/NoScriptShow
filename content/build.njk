---js
const eleventyNavigation = {
	key: "Build",
	order: 4
};
---
<h1>Build</h1>
<p><strong> Longer YouTube content where we design and build websites.</strong></p>

<ol reversed >
{%- for post in collections['build'] | reverse -%}
  <li><a href="{{post.url}}">{{ post.data.title }}</a><br> {{post.data.description}}</li>
{%- endfor -%}
</ol>